import LiveDashboard from '@/components/LiveDashboard';

export default function DashboardPage() {
    return (
        <main className="p-8 bg-[#fcfcfc] min-h-screen">
            <LiveDashboard />
        </main>
    );
}
