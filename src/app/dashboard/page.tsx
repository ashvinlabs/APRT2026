import LiveDashboard from '@/components/LiveDashboard';

export default function DashboardPage() {
    return (
        <main style={{ padding: '2rem 0', background: '#f0f2f5', minHeight: '100vh' }}>
            <LiveDashboard />
        </main>
    );
}
